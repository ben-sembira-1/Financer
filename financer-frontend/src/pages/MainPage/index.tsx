import { Typography, Stack } from "@mui/material";
export function MainPage() {
	return (
		<Stack height={"100vh"} justifyContent={"start"}>
			<Typography variant="h1">Financer</Typography>
			<Typography variant="h2">Monthly Tracker</Typography>
		</Stack>
	);
}
